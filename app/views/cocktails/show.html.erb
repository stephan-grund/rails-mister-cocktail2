<h1><%= @cocktail.name %></h1>

<h1><% @cocktail.doses.each do |dose| %></h1>
<h2><%= dose.description %></h2>
<h2><%= dose.ingredient.name %></h2>
<h2><%= link_to 'Delete',dose_path(dose), method: :delete, data: { confirm: 'Are you sure?'}  %></h2>
<% end %>

<%= link_to('Add a dose', new_cocktail_dose_path(@cocktail)) %>

<%= link_to('Back to overview', cocktails_path) %>
